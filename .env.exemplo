# Copie este arquivo para .env e preencha com seus valores
# cp .env.example .env

# Porta do servidor
PORT=3000

# JWT_SECRET=     (será adicionado no Bloco 4)
# DB_PATH=        (será adicionado no Bloco 3)